PORT=5001
NODE_ENV=development
LOG_LEVEL=info

# MongoDB connection
MONGO_URI=mongodb://localhost:27017/morongwa

# JWT secret for auth tokens (change in production)
JWT_SECRET=change-me-please

# URLs
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:5001

# PayGate configuration (set real values in production)
PAYGATE_ID=
PAYGATE_SECRET=
PAYGATE_URL=https://secure.paygate.co.za/payweb3/process.trans

# Email / Mailer configuration
# Set MAILER=ethereal for a developer-friendly test account (emails won't be sent to real addresses)
# Or set SMTP_HOST/SMTP_PORT/SMTP_USER/SMTP_PASS for a real SMTP provider
MAILER=ethereal
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=