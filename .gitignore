### Node / Next.js
node_modules/
.next/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Credentials (never commit)
SUPERADMIN_CREDENTIALS.md
**/*credentials*.json
**/credentials.json
*.pem
*.p12
*.pfx
*.keystore
*.jks
**/*.jks
google-play-service-account.json
**/google-play-service-account.json

# Env files (never commit secrets - keep .env out of version control)
.env
.env.local
.env.development
.env.production
.env.*
!.env.example
!**/.env.production.example
!**/.env.deploy.example
# Deploy SSH / host config (may contain passwords)
deploy-server.config
deploy-server.secrets
**/deploy-server.config
**/deploy-server.secrets
!deploy-server.config.example
!deploy-server.secrets.example
# App secrets at any depth
**/.env
**/.env.local
**/.env.development
**/.env.production
backend/.env
backend/.env.*
frontend/.env
frontend/.env.*
mobile/.env
mobile/.env.*
.env.development.local
.env.test.local
.env.production.local

# Logs
logs/
*.log
debug.log*

# OS
.DS_Store
Thumbs.db

# VS Code
.vscode/

# Expo local state
.expo/
mobile/.expo/

# Lock files
package-lock.json
pnpm-lock.yaml
yarn.lock

# Local deploy artifacts (regenerate with scripts/publish-frontend-test.ps1)
morongwa-frontend-only.tgz
morongwa-backend-deploy.tgz
_morongwa_full_deploy.tgz
/*.tgz
/*.tar.gz
_pack_frontend/
_pack_backend/

# Local backups / temp clutter (keep archives outside the repo)
Qwertymates-backups/
.tmp-*
*.tmp.html

# Local script/export state (PII, import batches, task logs)
backend/exports/

# Accidental Android bundle unpack (never commit)
mobile/BUNDLE-METADATA/
mobile/META-INF/
mobile/base/
mobile/BundleConfig.pb
