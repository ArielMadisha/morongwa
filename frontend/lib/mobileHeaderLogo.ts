/**
 * Same PNG as wall / logged-in home mobile header (not qwertyworld-icon / sidebar marks).
 * Keeps one canonical asset for AppShell + hub title rows.
 */
export const MOBILE_HEADER_LOGO_SRC = '/qwerty-home-icon.png';

/**
 * Shared circular logo sizing for sticky headers (mobile / compact).
 */
export const MOBILE_Q_LOGO_SIZE_CLASS = 'h-10 w-10 sm:h-11 sm:w-11 object-contain shrink-0';
